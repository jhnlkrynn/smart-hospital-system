<x-app-layout><x-slot name="header"><h2 class="text-2xl font-semibold text-gray-950">{{ $certificate->certificate_number }}</h2></x-slot><div class="py-8"><div class="mx-auto max-w-3xl space-y-6 px-4"><x-alert type="success" :message="session('status')" /><section class="rounded-lg border bg-white p-6 shadow-sm"><div class="flex justify-between"><h3 class="text-lg font-semibold">Medical Certificate</h3><x-status-badge :status="$certificate->status->value" /></div><p class="mt-4 text-sm"><strong>Patient:</strong> {{ $certificate->patient?->full_name }}</p><p class="text-sm"><strong>Purpose:</strong> {{ $certificate->purpose }}</p><p class="mt-4 whitespace-pre-line text-sm">{{ $certificate->clinical_summary }}</p><p class="mt-4 whitespace-pre-line text-sm">{{ $certificate->recommendation }}</p></section><form method="POST" action="{{ route('doctor.consultations.certificates.issue', [$consultation, $certificate]) }}">@csrf<x-primary-button>Issue</x-primary-button></form><form method="POST" action="{{ route('doctor.consultations.certificates.void', [$consultation, $certificate]) }}" class="space-y-2">@csrf<textarea name="reason" class="w-full rounded-md border-gray-300" placeholder="Void reason"></textarea><x-secondary-button>Void</x-secondary-button></form></div></div></x-app-layout>
