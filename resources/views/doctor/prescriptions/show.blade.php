<x-app-layout><x-slot name="header"><h2 class="text-2xl font-semibold text-gray-950">{{ $prescription->prescription_number }}</h2></x-slot><div class="py-8"><div class="mx-auto max-w-5xl space-y-4 px-4"><div class="rounded-lg border bg-white p-5 shadow-sm"><div class="flex items-start justify-between"><div><h3 class="font-semibold">{{ $prescription->patient?->full_name }}</h3><p class="text-sm text-gray-600">{{ $prescription->status->label() }}</p></div>@if($prescription->status->isEditable())<form method="POST" action="{{ route('doctor.prescriptions.finalize', $prescription) }}">@csrf<x-primary-button>Finalize</x-primary-button></form>@endif</div></div>@if($prescription->allergyWarnings->isNotEmpty())<div class="rounded-lg border border-red-200 bg-red-50 p-5 text-sm text-red-900"><h3 class="font-semibold">Allergy Warnings</h3>@foreach($prescription->allergyWarnings as $warning)<div class="mt-3 border-t border-red-200 pt-3"><p>{{ $warning->message }}</p>@unless($warning->acknowledged_at)<form method="POST" action="{{ route('doctor.prescriptions.warnings.acknowledge', [$prescription, $warning]) }}" class="mt-2 flex gap-2">@csrf<input name="override_reason" class="flex-1 rounded-md border-red-200" placeholder="Override reason"><button class="rounded-md bg-red-700 px-3 py-2 text-white">Acknowledge</button></form>@endunless</div>@endforeach</div>@endif<div class="rounded-lg border bg-white p-5 shadow-sm"><h3 class="font-semibold">Items</h3>@foreach($prescription->items as $item)<div class="mt-3 rounded border p-3 text-sm"><div class="font-medium">{{ $item->generic_name_snapshot }} {{ $item->brand_name_snapshot ? "({$item->brand_name_snapshot})" : '' }}</div><div>{{ $item->dose_quantity }} {{ $item->dose_unit_snapshot }} {{ $item->route_snapshot }} {{ $item->frequency_snapshot }} for {{ $item->duration_value }} {{ $item->duration_unit }}</div><div>Qty: {{ $item->quantity }} {{ $item->quantity_unit_snapshot }}</div><div class="text-gray-600">{{ $item->instructions }}</div></div>@endforeach</div>@unless($prescription->status->isTerminal())<form method="POST" action="{{ route('doctor.prescriptions.cancel', $prescription) }}" class="rounded-lg border bg-white p-5 shadow-sm">@csrf<textarea name="reason" class="w-full rounded-md border-gray-300" placeholder="Cancellation reason"></textarea><button class="mt-2 rounded-md bg-red-700 px-3 py-2 text-white">Cancel Prescription</button></form>@endunless</div></div></x-app-layout>
