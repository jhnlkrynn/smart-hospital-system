<x-app-layout><x-slot name="header"><h2 class="text-2xl font-semibold text-gray-950">Request Laboratory Tests</h2></x-slot><div class="py-8"><div class="mx-auto max-w-4xl px-4"><form method="POST" action="{{ route('doctor.consultations.laboratory-requests.store', $consultation) }}" class="space-y-4 rounded-lg border bg-white p-5 shadow-sm">@csrf<h3 class="font-semibold">{{ $consultation->patient?->full_name }}</h3><p class="text-sm text-gray-600">Laboratory results must be interpreted by an authorized healthcare professional together with the patient's clinical history and condition.</p><select name="priority" class="rounded-md border-gray-300"><option value="routine">Routine</option><option value="urgent">Urgent</option><option value="stat">STAT</option></select><div class="grid gap-2 md:grid-cols-2">@foreach($tests as $test)<label class="rounded border p-3 text-sm"><input type="checkbox" name="laboratory_test_ids[]" value="{{ $test->id }}"> <span class="font-semibold">{{ $test->code }}</span> {{ $test->name }}</label>@endforeach</div><textarea name="clinical_information" class="w-full rounded-md border-gray-300" placeholder="Clinical information"></textarea><textarea name="provisional_diagnosis" class="w-full rounded-md border-gray-300" placeholder="Provisional diagnosis"></textarea><textarea name="special_instructions" class="w-full rounded-md border-gray-300" placeholder="Special instructions"></textarea><x-primary-button>Create Request</x-primary-button></form></div></div></x-app-layout>
