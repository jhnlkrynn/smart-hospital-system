<x-app-layout><x-slot name="header"><h2 class="text-2xl font-semibold text-gray-950">{{ $laboratoryRequest->request_number }}</h2></x-slot><div class="py-8"><div class="mx-auto max-w-5xl space-y-6 px-4"><x-alert type="success" :message="session('status')" /><section class="rounded-lg border bg-white p-5 shadow-sm"><div class="flex justify-between"><div><h3 class="font-semibold">{{ $laboratoryRequest->patient?->full_name }}</h3><p class="text-sm text-gray-500">{{ $laboratoryRequest->priority->label() }}</p></div><x-status-badge :status="$laboratoryRequest->status->value" /></div><p class="mt-4 text-sm text-gray-600">Laboratory results must be interpreted by an authorized healthcare professional together with the patient's clinical history and condition.</p></section><section class="rounded-lg border bg-white p-5 shadow-sm"><h3 class="font-semibold">Results</h3>@foreach($laboratoryRequest->items as $item)<div class="border-t py-3 text-sm"><span class="font-semibold">{{ $item->test_name_snapshot }}</span><span class="ml-2 text-gray-500">{{ $item->status->label() }}</span>@if($item->result)<p>{{ $item->result->numeric_value ?? $item->result->text_value ?? $item->result->qualitative_value }} {{ $item->result->unit }}</p><p class="text-gray-500">Flag: {{ $item->result->abnormal_flag->label() }}</p>@if($item->result->is_critical)<form method="POST" action="{{ route('doctor.laboratory-results.acknowledge', $item->result) }}">@csrf<x-secondary-button>Acknowledge Critical Result</x-secondary-button></form>@endif @endif</div>@endforeach</section></div></div></x-app-layout>
