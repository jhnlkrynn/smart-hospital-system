<x-app-layout><x-slot name="header"><h2 class="text-2xl font-semibold text-gray-950">My Schedule</h2></x-slot><div class="py-8"><div class="mx-auto max-w-5xl space-y-4 px-4"><section class="rounded-lg border bg-white p-6 shadow-sm"><h3 class="font-semibold">Available slots for {{ $date }}</h3><div class="mt-3 flex flex-wrap gap-2">@forelse($slots as $slot)<span class="rounded-md bg-gray-100 px-3 py-2 text-sm">{{ $slot['label'] }}</span>@empty<p class="text-sm text-gray-500">No available slots.</p>@endforelse</div></section><section class="rounded-lg border bg-white p-6 shadow-sm"><h3 class="font-semibold">Weekly schedules</h3><ul class="mt-3 space-y-2 text-sm">@foreach($doctor->doctorSchedules as $schedule)<li>{{ $schedule->day_of_week->label() }} {{ substr($schedule->start_time,0,5) }} - {{ substr($schedule->end_time,0,5) }}</li>@endforeach</ul></section></div></div></x-app-layout>
