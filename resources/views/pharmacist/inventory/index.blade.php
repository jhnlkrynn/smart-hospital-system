<x-app-layout><x-slot name="header"><h2 class="text-2xl font-semibold text-gray-950">Pharmacy Inventory</h2></x-slot><div class="py-8"><div class="mx-auto max-w-7xl space-y-6 px-4"><div class="overflow-hidden rounded-lg border bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-gray-50 text-left"><tr><th class="p-3">Medication</th><th class="p-3">Lot</th><th class="p-3">Location</th><th class="p-3">On Hand</th><th class="p-3">Reserved</th><th class="p-3">Status</th><th class="p-3">Actions</th></tr></thead><tbody class="divide-y">@foreach($batches as $batch)<tr><td class="p-3 font-medium">{{ $batch->medication?->display_name }}</td><td class="p-3">{{ $batch->lot_number }}<div class="text-xs text-gray-500">Exp {{ $batch->expiration_date?->toDateString() ?: 'N/A' }}</div></td><td class="p-3">{{ $batch->location?->name }}</td><td class="p-3">{{ $batch->quantity_on_hand }}</td><td class="p-3">{{ $batch->quantity_reserved }}</td><td class="p-3">{{ $batch->status->label() }}</td><td class="p-3"><form method="POST" action="{{ route('pharmacist.inventory.adjust', $batch) }}" class="grid gap-1">@csrf<select name="adjustment_type" class="rounded-md border-gray-300 text-xs"><option value="increase">Increase</option><option value="decrease">Decrease</option></select><input name="quantity" class="rounded-md border-gray-300 text-xs" placeholder="Qty"><input name="reason" class="rounded-md border-gray-300 text-xs" placeholder="Reason"><button class="rounded border px-2 py-1 text-xs">Adjust</button></form><form method="POST" action="{{ route('pharmacist.inventory.quarantine', $batch) }}" class="mt-2 flex gap-1">@csrf<input name="reason" class="w-28 rounded-md border-gray-300 text-xs" placeholder="Reason"><button class="rounded border px-2 py-1 text-xs">Quarantine</button></form><form method="POST" action="{{ route('pharmacist.inventory.unquarantine', $batch) }}" class="mt-1">@csrf<button class="text-xs text-indigo-700">Unquarantine</button></form></td></tr>@endforeach</tbody></table></div>{{ $batches->links() }}<div class="rounded-lg border bg-white p-5 shadow-sm"><h3 class="font-semibold">Recent Ledger</h3>@foreach($transactions as $txn)<div class="mt-2 text-sm">{{ $txn->transaction_number }} | {{ $txn->transaction_type->label() }} | {{ $txn->medication?->display_name }} | {{ $txn->quantity }}</div>@endforeach</div></div></div></x-app-layout>
