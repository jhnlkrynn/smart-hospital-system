<x-app-layout><x-slot name="header"><h2 class="text-2xl font-semibold text-gray-950">{{ $prescription->prescription_number }}</h2></x-slot><div class="py-8"><div class="mx-auto max-w-5xl space-y-4 px-4"><p class="rounded-md border border-amber-200 bg-amber-50 p-3 text-sm text-amber-900">Review the patient's recorded allergies before reserving medication stock. Dispensing is handled in Phase 11.</p><div class="rounded-lg border bg-white p-5 shadow-sm"><h3 class="font-semibold">{{ $prescription->patient?->full_name }}</h3><p class="text-sm text-gray-600">Doctor: {{ $prescription->doctor?->full_name }} | Status: {{ $prescription->status->label() }}</p><div class="mt-3 flex gap-2"><form method="POST" action="{{ route('pharmacist.prescriptions.review', $prescription) }}">@csrf<button class="rounded-md border px-3 py-2 text-sm">Mark Reviewed</button></form><form method="POST" action="{{ route('pharmacist.prescriptions.reserve', $prescription) }}">@csrf<x-primary-button>Reserve Stock</x-primary-button></form></div></div><div class="rounded-lg border bg-white p-5 shadow-sm"><h3 class="font-semibold">Items</h3>@foreach($prescription->items as $item)<div class="mt-3 rounded border p-3 text-sm"><div class="font-medium">{{ $item->generic_name_snapshot }}</div><div>Qty: {{ $item->quantity }} | Status: {{ $item->status->label() }}</div><div>{{ $item->instructions }}</div></div>@endforeach</div><div class="rounded-lg border bg-white p-5 shadow-sm"><h3 class="font-semibold">Reservations</h3>@forelse($prescription->reservations as $reservation)<div class="mt-3 rounded border p-3 text-sm"><div>{{ $reservation->medication?->display_name }} | Lot {{ $reservation->stockBatch?->lot_number }} | {{ $reservation->quantity_reserved }}</div><div class="text-gray-600">{{ $reservation->released_at ? 'Released' : 'Active' }}</div>@unless($reservation->released_at)<form method="POST" action="{{ route('pharmacist.reservations.release', $reservation) }}" class="mt-2 flex gap-2">@csrf<input name="reason" class="flex-1 rounded-md border-gray-300" placeholder="Release reason"><button class="rounded-md border px-3 py-2">Release</button></form>@endunless</div>@empty<p class="mt-3 text-sm text-gray-600">No stock reserved yet.</p>@endforelse</div></div></div></x-app-layout>
