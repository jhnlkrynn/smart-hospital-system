<x-app-layout><x-slot name="header"><h2 class="text-2xl font-semibold text-gray-950">Pharmacy Purchases</h2></x-slot><div class="py-8"><div class="mx-auto max-w-7xl space-y-4 px-4"><a class="rounded-md bg-gray-900 px-3 py-2 text-sm text-white" href="{{ route('pharmacist.purchases.create') }}">New Purchase</a><div class="overflow-hidden rounded-lg border bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-gray-50 text-left"><tr><th class="p-3">PO</th><th class="p-3">Supplier</th><th class="p-3">Status</th><th class="p-3"></th></tr></thead><tbody class="divide-y">@foreach($purchases as $purchase)<tr><td class="p-3 font-medium">{{ $purchase->purchase_number }}</td><td class="p-3">{{ $purchase->supplier?->name }}</td><td class="p-3">{{ $purchase->status->label() }}</td><td class="p-3 text-right"><a class="text-indigo-700" href="{{ route('pharmacist.purchases.show', $purchase) }}">Open</a></td></tr>@endforeach</tbody></table></div>{{ $purchases->links() }}</div></div></x-app-layout>
