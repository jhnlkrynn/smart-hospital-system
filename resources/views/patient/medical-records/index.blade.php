<x-app-layout><x-slot name="header"><h2 class="text-2xl font-semibold text-gray-950">Medical Records</h2></x-slot><div class="py-8"><div class="mx-auto max-w-5xl space-y-6 px-4"><section class="rounded-lg border bg-white p-5 shadow-sm"><h3 class="mb-4 font-semibold">Consultation Timeline</h3>@forelse($consultations as $consultation)<a class="block border-t py-3" href="{{ route('patient.medical-records.show', $consultation) }}"><span class="font-semibold">{{ $consultation->completed_at?->format('M d, Y') }}</span> {{ $consultation->department?->name }} with {{ $consultation->doctor?->full_name }}</a>@empty<p class="text-sm text-gray-500">No finalized medical records yet.</p>@endforelse</section><section class="rounded-lg border bg-white p-5 shadow-sm"><h3 class="mb-4 font-semibold">Issued Medical Certificates</h3>@forelse($certificates as $certificate)<p class="border-t py-3 text-sm"><span class="font-semibold">{{ $certificate->certificate_number }}</span> {{ $certificate->purpose }}</p>@empty<p class="text-sm text-gray-500">No issued certificates.</p>@endforelse</section>{{ $consultations->links() }}</div></div></x-app-layout>
