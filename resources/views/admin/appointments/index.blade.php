<x-app-layout>
    <x-slot name="header"><h2 class="text-2xl font-semibold text-gray-950">Appointments</h2></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl space-y-4 px-4"><x-alert :message="session('status')" /><div class="flex justify-between">@can('appointments.create-for-patient')<a href="{{ route('admin.appointments.create') }}" class="rounded-md bg-gray-950 px-4 py-2 text-sm font-semibold text-white">Create Appointment</a>@endcan</div><form class="grid gap-3 rounded-lg border bg-white p-4 md:grid-cols-5"><input name="search" value="{{ request('search') }}" placeholder="Search" class="rounded-md border-gray-300"><select name="status" class="rounded-md border-gray-300"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status') === $status->value)>{{ $status->label() }}</option>@endforeach</select><input type="date" name="from" value="{{ request('from') }}" class="rounded-md border-gray-300"><input type="date" name="to" value="{{ request('to') }}" class="rounded-md border-gray-300"><button class="rounded-md border px-4 py-2 text-sm">Filter</button></form><div class="overflow-hidden rounded-lg border bg-white shadow-sm"><table class="min-w-full divide-y text-sm"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left">Number</th><th class="px-4 py-3 text-left">Date</th><th class="px-4 py-3 text-left">Patient</th><th class="px-4 py-3 text-left">Doctor</th><th class="px-4 py-3 text-left">Status</th><th></th></tr></thead><tbody class="divide-y">@forelse($appointments as $appointment)<tr><td class="px-4 py-3 font-semibold">{{ $appointment->appointment_number }}</td><td class="px-4 py-3">{{ $appointment->formatted_date }} {{ $appointment->formatted_time_range }}</td><td class="px-4 py-3">{{ $appointment->patient?->full_name }}</td><td class="px-4 py-3">{{ $appointment->doctor?->full_name }}</td><td class="px-4 py-3"><x-status-badge :status="$appointment->status->value" /></td><td class="px-4 py-3 text-right"><a href="{{ route('admin.appointments.show', $appointment) }}" class="underline">View</a></td></tr>@empty<tr><td colspan="6" class="px-4 py-8 text-center text-gray-500">No appointments found.</td></tr>@endforelse</tbody></table></div>{{ $appointments->links() }}</div></div>
</x-app-layout>
