# Inventory Transaction Ledger

`pharmacy_inventory_transactions` records inventory movement and operational events with transaction number, medication, batch, location, transaction type, quantity, before/after quantities, reference model, reason, actor, and timestamp.

Ledger entries are append-only from application services. They support audit review and future inventory reports.
